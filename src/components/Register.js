import React from 'react';

const Register = () => {
    return (
        <div>
            {/* <form onSubmit={handleRegister}>
        <input onBlur={handleEmailBlur} type="email" placeholder='Your Email' name="email"/><br />
        <input onChange={handlePasswordChange} type="password" placeholder='Your Password' name="password" /><br />
        <button type="submit">Register</button>
      </form> */}
        </div>
    );
};

export default Register;